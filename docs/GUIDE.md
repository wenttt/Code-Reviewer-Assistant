# AI Code Reviewer - 使用指南

## 目录

1. [快速开始](#1-快速开始)
2. [详细配置](#2-详细配置)
3. [功能说明](#3-功能说明)
4. [API Key获取指南](#4-api-key获取指南)
5. [常见问题](#5-常见问题)
6. [本地开发](#6-本地开发)

---

## 1. 快速开始

### 1.1 三步开始使用

```
步骤1: 获取 GitHub Token
步骤2: 获取 AI模型的 API Key (推荐 DeepSeek 或 Claude)
步骤3: 打开应用，配置Token，选择PR，开始审查
```

### 1.2 最小配置示例

```yaml
GitHub Token: ghp_xxxxxxxxxxxxxxxxxxxx
DeepSeek API Key: sk-xxxxxxxxxxxxxxxxxxxx
```

配置完成后，选择仓库 → 选择PR → 点击"开始AI代码审查"

---

## 2. 详细配置

### 2.1 GitHub Token 配置

#### 为什么需要GitHub Token？
- 读取您的仓库列表
- 获取PR的详细信息和代码diff
- 获取完整文件内容（用于上下文分析）

#### 需要的权限
- `repo` - 完整仓库访问权限（私有仓库必需）
- `public_repo` - 仅公开仓库访问（如果只审查公开仓库）

#### 创建步骤
1. 登录GitHub，进入 **Settings**
2. 左侧菜单选择 **Developer settings**
3. 选择 **Personal access tokens** → **Tokens (classic)**
4. 点击 **Generate new token** → **Generate new token (classic)**
5. 填写Token描述，如 "AI Code Reviewer"
6. 勾选 `repo` 权限
7. 点击 **Generate token**
8. 立即复制Token，关闭页面后无法再次查看

### 2.2 AI模型配置

#### 方案A: DeepSeek (推荐，性价比高)

**优点:**
- 价格便宜
- 代码理解能力强
- 中文支持好
- API稳定

**配置:**
```yaml
Provider: DeepSeek
API Key: sk-xxxxxxxxxxxx (从platform.deepseek.com获取)
Model: deepseek-chat (推荐) / deepseek-coder / deepseek-reasoner (R1推理)
```

#### 方案B: OpenAI

**优点:**
- 综合能力最强
- 生态成熟

**配置:**
```yaml
Provider: OpenAI
API Key: sk-xxxxxxxxxxxx
Model: gpt-5 (最强) / gpt-4.1 / gpt-4.1-mini / o3 (推理) / o4-mini
Base URL: 留空 或 自定义代理地址
```

#### 方案C: Anthropic Claude

**优点:**
- 代码审查能力极强
- 擅长发现深层逻辑问题
- 安全可控，拒绝不当内容

**配置:**
```yaml
Provider: Anthropic Claude
API Key: sk-ant-xxxxxxxxxxxx (从console.anthropic.com获取)
Model: claude-sonnet-4 (推荐) / claude-opus-4 (最强) / claude-3.7-sonnet / claude-3.5-haiku (快速)
```

#### 方案D: Google Gemini

**优点:**
- 超长上下文窗口（100万+ tokens），非常适合大PR
- 多模态支持
- Google AI Studio 免费额度

**配置:**
```yaml
Provider: Google Gemini
API Key: AIzaxxxxxx (从aistudio.google.com获取)
Model: gemini-2.5-pro (最强) / gemini-2.5-flash (推荐) / gemini-2.0-flash (快速)
```

#### 方案E: Ollama (本地模型)

**优点:**
- 完全免费
- 数据不出本地，绝对安全
- 无需网络

**配置:**
```yaml
Provider: Ollama
Base URL: http://localhost:11434
Model: qwen3 (推荐) / deepseek-r1 / codellama / llama4 / gemma3 / devstral
```

**前置要求:**
1. 安装Ollama: https://ollama.ai/download
2. 拉取模型: `ollama pull qwen3`
3. 启动服务: `ollama serve`

#### 方案F: 自定义API (公司内部AI服务)

**优点:**
- 接入公司内部已部署的AI服务
- 数据完全在公司内网流转
- 兼容所有OpenAI Chat Completions API格式的服务

**配置:**
```yaml
Provider: 自定义API
Base URL: https://your-company.com/api/v1 (必填)
API Key: your-api-key (如需要)
Model: your-model-name (必填，如 company-gpt、internal-llm)
```

**适用场景:**
- 公司内部部署的vLLM / TGI / Triton等推理服务
- Azure OpenAI Service
- 自建的API网关/代理
- 任何兼容OpenAI接口格式的第三方服务

### 2.3 高级选项

| 选项 | 默认值 | 说明 |
|------|--------|------|
| 敏感信息过滤 | 开启 | 自动检测并脱敏API Key、密码等敏感信息 |
| 上下文增强 | 开启 | 获取完整文件内容，提供更准确的审查 |

---

## 3. 功能说明

### 3.1 审查流程

```
┌─────────────────────────────────────────────────────────┐
│                    审查流程                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 验证Token → 获取用户信息和仓库列表                    │
│                    |                                    │
│  2. 选择仓库 → 获取该仓库的PR列表                        │
│                    |                                    │
│  3. 选择PR → 分析PR复杂度，显示预览                      │
│                    |                                    │
│  4. 选择AI提供商 → 配置API Key和模型                     │
│                    |                                    │
│  5. 开始审查:                                           │
│     a. 获取PR详情和所有文件的diff                        │
│     b. 敏感信息过滤（可选）                              │
│     c. 大PR自动分片                                     │
│     d. 获取完整文件上下文（可选）                        │
│     e. 调用AI模型进行审查                               │
│     f. 聚合结果并展示                                   │
│                    |                                    │
│  6. 查看审查报告                                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 审查维度

| 维度 | 权重 | 检查内容 |
|------|------|----------|
| **代码质量** | 30% | 可读性、命名规范、代码结构、注释 |
| **潜在Bug** | 25% | 逻辑错误、边界条件、异常处理、类型安全 |
| **安全问题** | 20% | 输入验证、注入攻击、敏感信息、权限控制 |
| **性能问题** | 15% | 算法效率、资源使用、查询优化、缓存 |
| **最佳实践** | 10% | 设计模式、代码复用、测试覆盖 |

### 3.3 评分标准

| 分数 | 等级 | 说明 |
|------|------|------|
| 90-100 | 优秀 | 代码质量高，几乎无问题 |
| 80-89 | 良好 | 有少量小问题 |
| 70-79 | 一般 | 有一些需要改进的地方 |
| 60-69 | 较差 | 存在明显问题 |
| 0-59 | 需重构 | 需要重大修改 |

### 3.4 问题严重程度

| 级别 | 图标 | 说明 |
|------|------|------|
| Critical | 🔴 | 严重问题，必须修复（如安全漏洞、严重Bug） |
| Major | 🟠 | 重要问题，建议修复（如逻辑问题、性能隐患） |
| Minor | 🟡 | 小问题，可选修复（如代码风格、命名不规范） |
| Info | 🔵 | 提示信息（如改进建议、最佳实践） |

### 3.5 安全过滤

自动检测并脱敏以下敏感信息：

```
- API Keys: OpenAI (sk-xxx), GitHub (ghp_xxx), AWS (AKIA...)
- 密码: password=xxx, db_pass=xxx
- Token: JWT, access_token, bearer token
- 私钥: RSA/SSH PRIVATE KEY
- 连接字符串: mongodb://user:pass@host
- 内网IP: 10.x.x.x, 192.168.x.x, 172.16-31.x.x
```

### 3.6 大PR处理

当PR包含大量文件时，系统会自动：

1. **分类文件**
   - 🔴 Critical: 核心业务代码、安全相关
   - 🟠 High: 重要代码文件
   - 🟡 Medium: 配置文件
   - 🟢 Low: 测试、文档

2. **过滤跳过**
   - package-lock.json, yarn.lock
   - 构建产物 (dist/, build/)
   - 自动生成文件 (.generated.)
   - 图片、字体等资源文件

3. **智能分片**
   - 每片最多8000 tokens
   - 按优先级排序，重要文件先审
   - 自动聚合多个分片的结果

---

## 4. API Key获取指南

### 4.1 DeepSeek API Key

1. 访问 https://platform.deepseek.com/
2. 注册/登录账号
3. 进入 **API Keys** 页面
4. 点击 **创建 API Key**
5. 复制生成的Key

### 4.2 OpenAI API Key

1. 访问 https://platform.openai.com/
2. 注册/登录账号
3. 进入 **API Keys** 页面
4. 点击 **Create new secret key**
5. 复制生成的Key

### 4.3 Anthropic Claude API Key

1. 访问 https://console.anthropic.com/
2. 注册/登录账号
3. 进入 **Settings** → **API Keys**
4. 点击 **Create Key**
5. 复制生成的Key (sk-ant-xxx)

### 4.4 Google Gemini API Key

1. 访问 https://aistudio.google.com/apikey
2. 登录Google账号
3. 点击 **Create API Key**
4. 选择项目并创建
5. 复制生成的Key (AIza...)

### 4.5 GitHub Token

1. 访问 https://github.com/settings/tokens
2. 点击 **Generate new token (classic)**
3. 勾选 `repo` 权限
4. 点击 **Generate token**
5. 复制生成的Token

---

## 5. 常见问题

### Q1: 审查时间很长怎么办？

**原因**: PR包含大量文件，需要分多片审查

**解决方案**:
- 等待完成，系统会自动分片处理
- 查看"PR分析预览"了解预计时间
- 大PR建议拆分成小PR提交
- 使用Gemini 2.5 Pro，其超长上下文可以减少分片次数

### Q2: 提示"Token无效"？

**检查**:
- GitHub Token是否已过期
- Token是否有`repo`权限
- 是否正确复制了完整Token

### Q3: AI审查结果不准确？

**建议**:
- 开启"上下文增强"获取更多信息
- 使用更强的模型 (GPT-5 > Claude Opus 4 > Gemini 2.5 Pro > DeepSeek Chat)
- 确保PR描述清晰，说明变更目的

### Q4: 担心代码安全怎么办？

**方案**:
1. 开启"敏感信息过滤"（默认开启）
2. 使用Ollama本地模型，代码不出本地
3. 使用自定义API接入公司内部AI服务
4. 使用私有部署的AI服务

### Q5: 如何接入公司内部的AI服务？

**步骤**:
1. 选择"自定义API"提供商
2. 填写公司AI服务的 API Base URL (需兼容OpenAI接口)
3. 填写 API Key (如果需要认证)
4. 填写模型名称 (你们部署的模型名)
5. 开始审查

**兼容的服务**: vLLM, Text Generation Inference (TGI), Triton, FastChat, LiteLLM, 以及任何实现了 OpenAI Chat Completions API 的服务。

### Q6: 支持哪些编程语言？

**完整支持** (有专门的解析器):
- Python, JavaScript/TypeScript
- Java, Go, Rust
- C/C++, C#, Ruby, PHP

**基础支持** (通用代码审查):
- 所有文本格式的代码文件

---

## 6. 本地开发

### 6.1 环境要求

```bash
Python 3.9+
Node.js 18+
```

### 6.2 安装依赖

```bash
# 后端
cd backend
pip install -r requirements.txt

# 前端
cd frontend
npm install
```

### 6.3 启动服务

```bash
# 终端1: 启动后端
cd backend
python main.py
# 服务运行在 http://localhost:8000

# 终端2: 启动前端
cd frontend
npm run dev
# 服务运行在 http://localhost:3000
```

### 6.4 项目结构

```
ai-code-reviewer/
├── backend/
│   ├── main.py              # FastAPI主应用
│   ├── github_client.py     # GitHub API客户端
│   ├── ai_reviewer.py       # AI审查引擎 (6种提供商)
│   ├── security.py          # 敏感信息过滤
│   ├── chunker.py           # 大PR分片处理
│   ├── context_analyzer.py  # 上下文分析
│   └── requirements.txt     # Python依赖
├── frontend/
│   ├── src/
│   │   ├── App.jsx          # React主组件
│   │   ├── main.jsx         # 入口文件
│   │   └── index.css        # 样式
│   ├── index.html
│   └── package.json
├── docs/
│   ├── PRD.md               # 产品需求文档
│   └── GUIDE.md             # 使用指南（本文档）
└── README.md
```

### 6.5 API端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/auth/validate` | POST | 验证GitHub Token |
| `/api/repos` | GET | 获取仓库列表 |
| `/api/repos/{owner}/{repo}/pulls` | GET | 获取PR列表 |
| `/api/repos/{owner}/{repo}/pulls/{number}` | GET | 获取PR详情 |
| `/api/repos/{owner}/{repo}/pulls/{number}/analyze` | GET | 分析PR复杂度 |
| `/api/review` | POST | 执行AI审查 |
| `/api/security/check` | POST | 检查敏感信息 |
| `/api/models/providers` | GET | 获取模型提供商列表 |

---

## 更新日志

### v3.0.0 (2026-02)
- 新增 Anthropic Claude 模型支持 (Sonnet 4 / Opus 4 / 3.7 Sonnet / 3.5 Haiku)
- 新增 Google Gemini 模型支持 (2.5 Pro / 2.5 Flash / 2.0 Flash)
- 新增 自定义API支持 - 可接入公司内部AI服务
- 更新 OpenAI 模型列表 (GPT-5 / GPT-4.1 / o3 / o4-mini)
- 更新 DeepSeek 模型列表 (新增 deepseek-reasoner R1)
- 更新 Ollama 本地模型列表 (Qwen3 / Llama4 / DeepSeek-R1 / Gemma3 / Devstral)
- 优化 前端UI，6种提供商切换面板

### v2.0.0 (2024-02)
- 新增 DeepSeek 模型支持
- 新增敏感信息自动过滤
- 新增大PR智能分片
- 新增完整文件上下文
- 新增 Ollama 本地模型支持
- 优化前端界面

### v1.0.0 (2024-02)
- 初始版本发布
- GitHub集成
- AI代码审查
- 基础界面

---

**有问题或建议？欢迎提Issue!**
